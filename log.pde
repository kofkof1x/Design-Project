class Log {
  int currentposx;
  int currentposy;
  int loglength;
  int logheight = 30;
  int Logvelocity = 3;

  public Log(int x, int y) {
    this.currentposx = new currentposx(0);
    this.currentposy = new currentposy(125);
    this.loglength = new loglength(random(70, 150));
  }

  //public boolean touching(float x, float y) {

  //}
}
